{
  "user": {
    "plan": "max20x",
    "comment": "Options: pro, max5x, max20x - Set this to your Claude subscription plan"
  },
  "ccusage_version": "17.1.0",
  "limits": {
    "weekly": {
      "pro": 300,
      "max5x": 500,
      "max20x": 850,
      "comment": "Weekly cost limits in USD for each plan tier. NOTE: pro and max5x limits are estimated/unverified - only max20x has been tested"
    },
    "context": 168,
    "cost": 140,
    "token": 220000,
    "comment": "Context limit (in thousands), cost limit per 5-hour session, token limit for tracking"
  },
  "paths": {
    "claude_projects": "~/.claude/projects/",
    "comment": "Path to your Claude Code projects directory"
  },
  "display": {
    "bar_length": 10,
    "transcript_tail_lines": 200,
    "session_activity_threshold_minutes": 5,
    "comment": "Display settings: progress bar length, lines to read from transcript, minutes for active session detection"
  },
  "colors": {
    "orange": "\\033[1;93m",
    "red": "\\033[31m",
    "pink": "\\033[38;5;225m",
    "green": "\\033[38;5;194m",
    "purple": "\\033[35m",
    "cyan": "\\033[96m",
    "reset": "\\033[0m",
    "comment": "ANSI color codes for statusline elements. Remove '1;' prefix for normal weight, add '1;' for bold (e.g., \\033[1;31m)"
  },
  "multi_layer": {
    "layer1": {
      "threshold_percent": 30,
      "color": "green",
      "comment": "Layer 1: 0-30% usage. Color options: green, orange, red, pink, purple, cyan"
    },
    "layer2": {
      "threshold_percent": 50,
      "color": "orange",
      "comment": "Layer 2: 30-50% usage. Color options: green, orange, red, pink, purple, cyan"
    },
    "layer3": {
      "threshold_percent": 100,
      "color": "red",
      "comment": "Layer 3: 50-100% usage. Color options: green, orange, red, pink, purple, cyan"
    },
    "comment": "Thresholds define when color changes. Colors must match keys in 'colors' section. Scale multipliers auto-calculated."
  },
  "daily_layer": {
    "layer1": {
      "threshold_percent": 14.29,
      "color": "green",
      "comment": "Layer 1: 0-14.29% of weekly limit (normal daily usage, 1/7 of weekly)"
    },
    "layer2": {
      "threshold_percent": 21.44,
      "color": "orange",
      "comment": "Layer 2: 14.29-21.44% of weekly limit (exceeding daily usage, 1.5x expected). Shows overage status."
    },
    "comment": "Daily usage thresholds. Layer 1 = expected daily usage (1/7 weekly). Layer 2 = 1.5x daily limit for overage tracking. Multipliers auto-calculated."
  },
  "sections": {
    "show_directory": true,
    "show_context": true,
    "show_five_hour_window": true,
    "show_daily": true,
    "show_weekly": true,
    "show_timer": true,
    "show_token_rate": true,
    "show_sessions": true,
    "comment": "Toggle visibility of statusline sections - set to false to hide any section. show_directory: project name (default on, bold bright orange). show_five_hour_window: 5-hour cost window ($X/$Y [bar] %). show_daily: daily cycle tracker. show_weekly: weekly percentage. show_timer: reset countdown. show_token_rate: current token burn rate (tokens/min). show_sessions: active session count."
  },
  "tracking": {
    "weekly_scheme": "ccusage",
    "official_reset_date": "",
    "weekly_baseline_percent": 0,
    "cache_duration_seconds": 300,
    "comment": "Weekly tracking: 'ccusage' (default, uses ISO weeks) or 'ccusage_r' (optional, matches Anthropic console schedule). To use ccusage_r: set official_reset_date from your console's 'Resets [date/time]' (format: 2025-10-08T15:00:00-07:00 for 'Oct 8, 3pm Vancouver'). weekly_baseline_percent compensates for untracked costs from deleted transcripts (clear/compact commands) - set to the percentage gap between your Anthropic console and statusline (e.g., console 15% vs statusline 10% → set to 5). cache_duration_seconds controls how long ccusage_r results are cached (default: 300 = 5 minutes). See README for setup guide."
  }
}
